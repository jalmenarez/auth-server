apply plugin: "war"

bootWar {
    mainClassName = "cl.jalmenarez.auth.AuthApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
